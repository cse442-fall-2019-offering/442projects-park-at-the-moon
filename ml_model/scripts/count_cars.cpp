#include <iostream>
#include "get_wheels.hpp"

// all operations in the server gathering wheel
// data from both cameras, comparing wheels, and 
// determining car count is done here

// TODO: we need a compare vectors function
//       we need to detect particular car given 2 wheels

float calculate_distance(std::vector<std::vector<float>> &wheel_path1, std::vector<std::vector<float>> &wheel_path2) {

    
}

// compare positive velocity vectors
// compare negative velocity vectors
int detect_car(Camera c1, Camera c2) {


}
